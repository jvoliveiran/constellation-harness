# Project Map

> Generated by `/constellation:init`. Agents read this file for orientation instead of
> exploring the codebase from scratch. Regenerate with `/constellation:init --refresh`
> whenever the module layout changes significantly.

## How to operate

When exploring this codebase to implement something:

1. **Start with Glob/Grep, NOT Read.** Only Read files you've confirmed are relevant.
2. **MAXIMUM 10 tool calls** before producing actionable output (code, a file list, or a concrete gap analysis). If you hit 10 tool calls without output, STOP exploring and produce output with what you have.
3. **Never read more than 3 files** for "orientation" — use the structure map below instead.

## Project Structure

<!-- init fills this in: an annotated tree of the source directories, one line per
     module/file group, with a short "→ purpose" note. Keep it under ~120 lines. -->

## File Naming Conventions

<!-- init fills this in: table of pattern → purpose → example, derived from the
     project's actual conventions. -->

## Finding Tests

<!-- init fills this in: glob patterns for unit / integration / e2e tests. -->

## Schema / Data Model

<!-- init fills this in: where the source-of-truth schema lives (e.g. prisma/schema.prisma,
     migrations dir, OpenAPI spec), or "not applicable". -->

## Useful Documents

- `README.md` — global project overview
- `.constellation/tasks/` — the work pipeline (unit of work; one task per workflow)
- `.constellation/plans/` — implementation plans, one per planned task (same number + slug)
- `.constellation/epics/` + `.constellation/features/` — work hierarchy above tasks
- `.constellation/artifacts/` — PRDs, spike findings, discovery notes
- `.constellation/adrs/` — architecture decision records

## Code Review

When reviewing code, **ALWAYS** use `git diff` to scope the review to the actual changes.
